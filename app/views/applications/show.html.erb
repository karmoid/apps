<div class="row">
  <div class="col-lg-12">
    <h2 id="type-blockquotes"><%= @application.name %></h2>
  </div>
</div>
<div class="row">
  <div class="col-lg-6">
    <div class="bs-component">
      <blockquote>
        <p><%= @application.note %></p>
        <span class="label label-info"><%= link_to "#" + @application.id.to_s, rails_admin.edit_path(model_name: "application", id: @application.id) %></span> <span class="label label-default"><%= @application.updated_at %></span>
      </blockquote>
    </div>
  </div>
  <div class="col-lg-6">
    <div class="bs-component">

    </div>
  </div>
</div>
<div class="row">
  <div class="col-lg-4">
    <div class="bs-component">
      <div class="panel panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title"><span class="badge"><%= @powers.count %></span> Rôles</h3>
        </div>
        <div class="panel-body">
          <ul class="list-group">
            <% @powers.each do |p| %>
              <li class="list-group-item">
                <b><%= link_to p.app_role.name, controller: p.app_role.class.name.tableize, action: :show, id: p.app_role.id, :search => params[:search] %></b> <%= p.app_role.note %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
      Résumé des rôles attribués à l'application.
    </div>
  </div>
  <div class="col-lg-4">
    <div class="bs-component">
      <div class="panel panel-success">
        <div class="panel-heading">
          <h3 class="panel-title"><span class="badge"><%= @app_modules.count %></span> Modules applicatifs</h3>
        </div>
        <div class="panel-body">
          <ul class="list-group">
            <% @app_modules.each do |p| %>
            <li class="list-group-item">
              <b><%= link_to p.name, controller: p.class.name.tableize, action: :show, id: p.id, :search => params[:search] %></b> <%= p.note %>
            </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <% unless @maintener.nil? %>
    <div class="col-lg-4">
      <div class="bs-component">
        <div class="panel panel-warning">
          <div class="panel-heading">
            <h3 class="panel-title"><span class="badge"><%= @mainteners.count %></span> Mainteneur <%= @maintener.name %></h3>
          </div>
          <div class="panel-body">
            <ul class="list-group">
              <% @mainteners.each do |p| %>
              <li class="list-group-item">
                <b><%= link_to p.name, controller: p.class.name.tableize, action: :show, id: p.id, :search => params[:search] %></b> <%= p.note %>
              </li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div class="row">
  <div class="col-lg-4">
    <div class="bs-component">
      <div class="panel panel-info">
        <div class="panel-heading">
          <h3 class="panel-title"><span class="badge"><%= @hostsprod.count %></span> Production</h3>
        </div>
        <div class="panel-body">
          <ul class="list-group">
            <% @hostsprod.each do |p| %>
              <li class="list-group-item">
                <b><%= link_to p.name, controller: p.class.name.tableize, action: :show, id: p.id, :search => params[:search] %></b> <%= p.note %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-4">
    <div class="bs-component">
      <div class="panel panel-info">
        <div class="panel-heading">
          <h3 class="panel-title"><span class="badge"><%= @hostspprod.count %></span> Pré Production</h3>
        </div>
        <div class="panel-body">
          <ul class="list-group">
            <% @hostspprod.each do |p| %>
            <li class="list-group-item">
              <b><%= link_to p.name, controller: p.class.name.tableize, action: :show, id: p.id, :search => params[:search] %></b> <%= p.note %>
            </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <div class="col-lg-4">
    <div class="bs-component">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title"><span class="badge"><%= @hostsdev.count %></span> Développement</h3>
        </div>
        <div class="panel-body">
          <ul class="list-group">
            <% @hostsdev.each do |p| %>
            <li class="list-group-item">
              <b><%= link_to p.name, controller: p.class.name.tableize, action: :show, id: p.id, :search => params[:search] %></b> <%= p.note %>
            </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
